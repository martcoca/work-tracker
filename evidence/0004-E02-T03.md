# Evidence: 0004-E02-T03

## Check

Run on 2026-08-30 from `codex/0004-e02-t03-v2`, with `GOWORK=off`,
`GOPROXY=off`, and a temporary Go build cache:

- `go test -count=1 ./...` passed. Every package with tests reported `ok`;
  `cmd/tracker-web` and `internal/mutationgate` reported `[no test files]`.
- `go vet ./...` produced no output and exited zero.
- `tofu test -no-color` in `infra/identity` reported
  `Success! 2 passed, 0 failed.` The clean mock plan and the floating-image
  rejection both passed.
- `docker build --tag work-tracker:check .` completed all 29 steps and tagged
  `work-tracker:check`.
- `scripts/container/check-workflow.sh` reported
  `PASS: image publication is keyless and confined to merged main revisions`.
  Both changed workflow files also parsed as YAML.
- `scripts/container/check-image.sh work-tracker:check` reported:

  ```text
  PASS: credential scan (3 layers, 85 regular files)
  PASS: credential scan self-test (dummy private-key refused in tmp/dummy-private-key.txt and removed)
  PASS: image structure (user=65532:65532 size_bytes=7334494 pinned_stages=3)
  ```

- `scripts/container/check-runtime.sh work-tracker:check` reported a healthy
  response with packet, tenant-directory, and agent-grants all
  `available:true` and `stale:false`, followed by:

  ```text
  identity=uid=65532 gid=65532 groups=65532
  PASS: production container served verified runtime exports
  ```

The desktop sandbox initially denied Go's loopback test listeners and execution of the
cached OpenTofu provider. The exact checks were rerun with local process/loopback access
and passed as recorded above. No plan contacted a cloud project and no apply ran.

## Image and runtime decisions

The three `FROM` instructions pin immutable multi-platform indexes:

- `golang:1.26.4-alpine3.23@sha256:18b460dd17542c2ba43299a633cf6ebfc1115101509531471d7cfce1019af083`
  supplies the pinned compiler and standard library in the discarded build stage.
- `alpine:3.23.3@sha256:25109184c71bdad752c8312a8623239686a9a2071e8825f20acb8f2198c3f659`
  supplies the small runtime filesystem and BusyBox health-check client.

The index pin lets an arm64 workstation use a native builder while the build explicitly
cross-compiles the binary for Linux amd64; the publish job selects `linux/amd64` directly
for Cloud Run. Pinning the amd64 child manifest for the build stage instead caused the Go
compiler itself to crash under local QEMU emulation, before repository code ran.

The 7,334,494-byte final image contains Alpine's minimal runtime files, CA roots, and the
stripped `tracker-web` binary. Mechanical checks prove the final user is numeric UID/GID
65532 and that `/usr/local/go`, `/src`, `/go/pkg/mod`, and `/root/.cache/go-build` are absent.
No export is copied into any production layer. A separate Docker target compiles the local
synthetic export server; that target is used only by the smoke test and never contributes a
production layer.

T04 made a bare `docker run` invalid because the service now refuses cold start without all
three verified exports. The nearest valid packet check starts a local synthetic HTTP fixture,
shares its network namespace with the unmodified production image, and then requests the
production `/healthz`. The response proves the production process fetched and verified all
three contracts rather than relying on data baked into the image.

Docker now exports gzip-compressed OCI layers, so the scanner detects and decompresses gzip
before walking every regular file. It fails closed on oversized files, reports rule/path but
never matching bytes, and its self-test creates a temporary child image containing a
synthetic private-key marker, proves rejection, and removes the child image.

## CI and unapplied plan

The `Image` workflow builds and inspects every pull request. Its publish job can run only on
`refs/heads/main` after the build succeeds, exchanges GitHub OIDC for a ten-minute Google
access token, logs in only to `vars.GCP_ARTIFACT_REPOSITORY`, and pushes an amd64 image tagged
with the merged commit SHA. It uses the four supplied repository variables and no stored
cloud secret. The registry repository is checked against `vars.GCP_PROJECT_ID`; no step asks
for registry deletion, configuration, state, IAM, or deploy permissions. The workflow writes
the full registry-returned `image@sha256:...` reference to its job summary.

The unapplied Cloud Run plan removes `PACKET_EXPORT_PATH` and `TENANT_DIRECTORY_PATH`. It now
sets the three HTTPS export URLs plus refresh/fetch timing, and it rejects any
`container_image` that is not a full lowercase `@sha256:` reference. No real identifier or
registry coordinate is committed.

## Blocker: published digest

No image was published and no immutable registry digest exists yet. Repository doctrine
forbids this worker from publishing, while the least-privilege workflow correctly refuses to
publish a pull-request revision and can run only after this work reaches `main`. The packet
also requires the published digest in this same evidence file before its pull request can
assert completion. Those conditions form a sequencing dependency that the worker cannot
resolve without either publishing on unauthorized authority or weakening the main-only rule.

The local image ID is deliberately not recorded as though it were a registry digest. The
branch is safe to review and its pull-request checks can build it, but packet status remains
`in progress` until the merged-main workflow publishes and its full digest can be handed to
the Founder for `container_image`.
