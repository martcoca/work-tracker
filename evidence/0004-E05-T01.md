# 0004-E05-T01 evidence

Status: blocked on Founder-applied state migration and workload-identity trust, followed by
one real merge. No cloud apply, publication, or deployment was performed by the worker.

## Remote state

The committed GCS prefixes place foundation state at
`gs://$GCP_STATE_BUCKET/work-tracker/foundation/default.tfstate` and delivery state at
`gs://$GCP_STATE_BUCKET/work-tracker/delivery/default.tfstate`. Trust state uses
`gs://$GCP_STATE_BUCKET/work-tracker/deploy-trust/default.tfstate` and remains
Founder-managed. The bucket name is a protected deployment coordinate and is not tracked.

`scripts/cloud/gcp/migrate-state.sh` first verifies Object Versioning, saves ignored local
backups, moves the existing `google_cloud_run_v2_service.reader` address into delivery
state without importing or recreating it, then migrates both local states. A fresh checkout
runs `scripts/cloud/gcp/check-remote-state.sh`; it verifies versioning, acquires the GCS
backend lock during a saved plan, and passes that plan through the deploy guard. Neither
Founder-run check has run yet, so the remote objects and second-checkout plan are not yet
claimed as live facts.

## Founder apply

The Founder must review and apply `infra/trust`. It creates a separate deploy service
account and admits exactly
`repo:<owner-id>/<repository-id>:ref:refs/heads/main` through the existing work-tracker
pool. The binding has no wildcard, prefix match, or pull-request subject. It grants:

- Cloud Run Developer on only `tracker-reader`;
- Artifact Registry Reader on only the configured image repository;
- Service Account User on only the zero-permission runtime account;
- Storage Object Admin on the existing state bucket, conditioned so object access covers
  only `work-tracker/delivery/**`. Bucket-level object listing is unavoidable for the GCS
  backend; foundation state containing the OAuth client secret remains unreadable;
- Firebase Hosting Admin, the narrowest supported Hosting write role because Firebase does
  not support custom Hosting roles; and
- API Keys Viewer, the read-only role Firebase documents as additionally required by its
  CLI.

It grants no image upload or deletion, second Cloud Run service creation, project IAM
administration, service-account key creation, Identity Platform administration, or state
bucket administration. After apply, the Founder must set `GCP_DEPLOYER_SERVICE_ACCOUNT`,
`GCP_STATE_BUCKET`, `GCP_REGION`, `GCP_HOSTING_SITE_ID`, and
`GCP_RUNTIME_SERVICE_ACCOUNT_NAME` as repository variables. The existing publisher
variables remain the only image-push authority.

## Checks run

The complete Go suite and vet passed, including strict runtime-export and delivery-guard
tests. All identity, delivery, and trust OpenTofu configurations validated; mock-provider
tests passed `1`, `3`, and `3` runs respectively. The backend-free foundation plan was
`12 to add, 0 to change, 0 to destroy`, followed by:

```text
PASS: cost guard (12 planned resources, idle cost zero)
```

The deployment policy and explicit refusal demonstration returned:

```text
PASS: deploy is keyless, exact-main, ordered, guarded, and digest-preserving
PASS: non-zero idle cost refused: ... configures min_instance_count=1
PASS: destructive delivery refused: ... forbidden actions [delete]
PASS: unverifiable authority refused: ... fetch request failed
PASS: workflow-permissions (4 workflows, all explicit)
PASS: automerge-coverage (3 workflow(s) awaited)
PASS: post-merge-workflows (all push workflows are dispatchable)
```

The exact packet scan found no workflow secret or key reference. Frontend results were
`5` files and `10` tests passed, followed by a successful production build. A local build
of the production container returned:

```text
PASS: credential scan (3 layers, 85 regular files)
PASS: credential scan self-test (dummy private-key refused and removed)
PASS: image structure (user=65532:65532 size_bytes=7337189 pinned_stages=3)
PASS: production container served verified runtime exports
```

## Live provenance still required

After the Founder steps and a real merge, the Deploy workflow must build once, scan and
exercise that image, push that same image, apply the registry-returned digest, release
Hosting, and read the merge SHA back from both surfaces. The packet's final command has not
run and must be added here with its actual output before status can become done:

```sh
gcloud run services describe tracker-reader --region=us-central1 \
  --format='value(spec.template.metadata.annotations)' | grep "$(git rev-parse HEAD)"
```
