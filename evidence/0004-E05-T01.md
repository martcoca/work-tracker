# 0004-E05-T01 evidence

Status: done. The Founder applied the state migration and workload-identity trust. A green
exact-main deployment and both public provenance surfaces were checked independently by the
worker. The worker performed no cloud apply, IAM mutation, or publication.

## Remote, versioned state

The migrated state is in the versioned `gs://martcoca-gcp-tfstate` bucket at these GCS
backend prefixes:

- `work-tracker/foundation/default.tfstate`
- `work-tracker/delivery/default.tfstate`
- `work-tracker/deploy-trust/default.tfstate`

The migration script originally could not split the state because both real roots already
declared a GCS backend and OpenTofu refused `state mv` before backend initialization. The
corrected script copies the foundation Terraform files except `backend.tf` into a temporary
root, initializes that root with `-backend=false`, and moves the Cloud Run address between
the two absolute local state paths. Only after that split does it initialize each real GCS
backend with `-migrate-state`. This reads correctly: it changes state ownership without an
import, recreation, or premature remote-backend dependency.

The exact-main Deploy run for commit `fc01596100453645aea977d5d25c50bc26f9b8ea`
completed successfully. Its fresh GitHub checkout initialized the delivery backend,
acquired the state lock, produced and guarded a saved plan, and applied that plan. This is
the independent checkout proof that the migrated state is readable and lockable.

## Applied exact-main trust

The Founder applied the `work-tracker-deployer` identity through the dedicated
`work-tracker` pool. Its workload-identity binding admits exactly:

```text
repo:martcoca@309195935/work-tracker@1349580381:ref:refs/heads/main
```

There is no wildcard, prefix match, or pull-request subject. The applied grants retain the
authored boundaries: service-scoped Cloud Run Developer on `tracker-reader`,
repository-scoped Artifact Registry Reader, Service Account User on the runtime identity, conditioned
delivery-state object access, Firebase Hosting Admin, and read-only API Keys Viewer. All
five delivery variables named by the workflow are set in the repository:
`GCP_DEPLOYER_SERVICE_ACCOUNT`, `GCP_STATE_BUCKET`, `GCP_REGION`,
`GCP_HOSTING_SITE_ID`, and `GCP_RUNTIME_SERVICE_ACCOUNT_NAME`. No credential is stored
there or in Actions.

## Least-privilege finding from the first deployment

The first authenticated deploy changed the service but failed while polling its own
long-running operation: service-scoped `roles/run.developer` does not include
`run.operations.get`, and operation resources cannot be scoped to the service. The applied
correction adds project-level `roles/run.viewer`, the narrowest predefined role containing
that read permission. It adds observation only. The deployer's only Cloud Run write remains
`roles/run.developer` on the single `tracker-reader` service. The subsequent exact-main
deployment completed green.

## Check output

The local packet checks returned:

```text
PASS: deploy path contains no secret reference, private key, or service-account JSON
PASS: workflow-permissions (4 workflows, all explicit)
GOWORK=off go test ./... -count=1
ok   github.com/martcoca/work-tracker/{authoring,cmd/publish-packet-export,
cmd/verify-runtime-exports,contract,identity,internal/containerfixture,
internal/costguard,internal/deployguard,internal/imagescan,packet,packetexport,
repositorypacket,runtimeexport,surface,tenant}
?    github.com/martcoca/work-tracker/cmd/tracker-web [no test files]
```

The earlier implementation evidence remains applicable. OpenTofu validation and
mock-provider tests passed for identity, delivery, and trust; the backend-free foundation
plan was `12 to add, 0 to change, 0 to destroy` and passed the zero-idle-cost guard. The
delivery and image demonstrations returned:

```text
PASS: deploy is keyless, exact-main, ordered, guarded, and digest-preserving
PASS: non-zero idle cost refused: ... configures min_instance_count=1
PASS: destructive delivery refused: ... forbidden actions [delete]
PASS: unverifiable authority refused: ... fetch request failed
PASS: credential scan (3 layers, 85 regular files)
PASS: credential scan self-test (dummy private-key refused and removed)
PASS: image structure (user=65532:65532 size_bytes=7337189 pinned_stages=3)
PASS: production container served verified runtime exports
```

The latest main Deploy run was independently read from GitHub Actions as `success` for
`fc01596100453645aea977d5d25c50bc26f9b8ea`. Using the repository's configured project and
region without printing them, the live Cloud Run annotation and public Hosting provenance
file returned:

```sh
CLOUDSDK_ACTIVE_CONFIG_NAME=martcoca gcloud run services describe tracker-reader \
  --project="$PROJECT_ID" --region="$REGION" \
  --format='value(spec.template.metadata.annotations)'
curl -fsS "https://tracker.martcoca.com/source-commit-$EXPECTED_COMMIT.txt"
```

The values read from those commands were:

```text
Cloud Run source-commit: fc01596100453645aea977d5d25c50bc26f9b8ea
Hosting source-commit: fc01596100453645aea977d5d25c50bc26f9b8ea
```

Both values equal the then-current `origin/main` commit. The live provenance requirement is
satisfied; no remaining check is deferred.
